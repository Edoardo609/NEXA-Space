import { admin, db } from 'hatchable';
export const access='admin';
export default async function(req,res){const p=await admin.profile(req);if(!p||p.email?.toLowerCase()!=='edoardodalsoggio@gmail.com')return res.status(403).json({error:'Owner access required'});const [unique,views,today,active,pages]=await Promise.all([db.query('SELECT count(DISTINCT member_id)::int AS n FROM analytics_events WHERE member_id IS NOT NULL'),db.query('SELECT count(*)::int AS n FROM analytics_events'),db.query('SELECT count(*)::int AS n FROM analytics_events WHERE created_at>=current_date'),db.query("SELECT count(DISTINCT member_id)::int AS n FROM analytics_events WHERE member_id IS NOT NULL AND created_at>=now()-interval '15 minutes'"),db.query('SELECT page,count(*)::int AS views FROM analytics_events GROUP BY page ORDER BY views DESC LIMIT 8')]);res.json({unique_users:unique.rows[0].n,views:views.rows[0].n,today:today.rows[0].n,active_15m:active.rows[0].n,pages:pages.rows})}