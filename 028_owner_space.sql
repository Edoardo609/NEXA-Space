CREATE TABLE IF NOT EXISTS owner_settings (owner_email TEXT PRIMARY KEY, first_name TEXT NOT NULL DEFAULT '', last_name TEXT NOT NULL DEFAULT '', display_name TEXT NOT NULL DEFAULT '', avatar_url TEXT NOT NULL DEFAULT '', bio TEXT NOT NULL DEFAULT '', language TEXT NOT NULL DEFAULT 'it', notification_email TEXT NOT NULL DEFAULT '', autosave BOOLEAN NOT NULL DEFAULT TRUE, updated_at TIMESTAMPTZ NOT NULL DEFAULT now())